# FTP Connection Settings
FTP_HOST=ftp.example.com
FTP_USER=username
FTP_PASS=password

# Local and Remote Paths
LOCAL_DIR=/downloads
REMOTE_DIR=/remote/path

# Transfer Settings
PARALLEL_JOBS=3
CHUNKS_PER_FILE=15